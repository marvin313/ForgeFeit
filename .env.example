# Reference template only. ForgeFit release builds use --dart-define values;
# this file is never bundled into the app.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-publishable-key
